import 'package:flutter/material.dart';

class MainViewScreen extends StatelessWidget {
  const MainViewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Tela Principal'),
    );
  }
}
